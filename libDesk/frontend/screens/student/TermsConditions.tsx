// Kept for backwards compatibility (if any old route navigates here).
// Student Settings now opens the legal URL inside `StudentLegalWebView`.
import StudentLegalWebViewScreen from './LegalWebView';

export default StudentLegalWebViewScreen;

